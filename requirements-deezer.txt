# Optional dependencies for Deezer FLAC downloads
# Install with: pip install -r requirements-deezer.txt

# Note: deemix is not on PyPI, install from source
# Manual installation commands:
# pip install git+https://gitlab.com/RemixDev/deemix-py.git
# pip install deezer-py

# Alternative: Use spotdl which supports multiple sources
spotdl==4.2.5

# Or use these for basic Deezer API access
deezer-python==6.1.0
requests==2.32.3
